@model MeterWeb.MeterType

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>MeterType</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MeterTypeId" />
            <div class="form-group">
                <label asp-for="MeterTypeName" class="control-label"></label>
                <input asp-for="MeterTypeName" class="form-control" />
                <span asp-validation-for="MeterTypeName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MeterServiceId" class="control-label"></label>
                <select asp-for="MeterServiceId" class="form-control" asp-items="ViewBag.MeterServiceId"></select>
                <span asp-validation-for="MeterServiceId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

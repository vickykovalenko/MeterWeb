@model IEnumerable<MeterWeb.Flat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FlatAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FlatAddress)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FlatId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FlatId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FlatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
